# Voice Agent Requirements

# Core dependencies
python-dotenv==1.0.0
pyyaml>=6.0.0

# Web server and WebSocket support
aiohttp>=3.9.0
websockets>=12.0

# HTTP client for Azure OpenAI Responses API
httpx>=0.25.0

# OpenAI SDK (for embeddings in news_tools.py)
openai>=1.0.0

# Azure Speech Services for STT and TTS
azure-cognitiveservices-speech>=1.40.0

# PDF form filling
pypdf>=4.0.0

# Retry logic for API calls
tenacity>=8.0.0

# TTL caching for API responses
cachetools>=5.0.0
